package com.robyn.dayplus2.data.source.enums

/**
 * For the 1st arg in [com.robyn.dayplus2.addEdit.AddEditPresenter.setMemeberVars()],
 *
 * represents each editable (not mUuid) field of an [MyEvent] instance.
 */
enum class EventField {

    EVENT_TITLE,

    EVENT_DESCRIPTION,

    EVENT_DATETIME,

    EVENT_REPEAT_MODE,

    EVENT_STAR,

    EVENT_CATEGORY,

    EVENT_IMAGE_PATH,
}